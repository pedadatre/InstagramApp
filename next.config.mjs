/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'lzjthiqyn89l05t2.public.blob.vercel-storage.com',
            's.gravatar.com',
            'lh3.googleusercontent.com'
        ]
    }
};

export default nextConfig;
